# 가비아에 샘플 페이지 추가하기

도메인(connectsell.co.kr)을 가비아에 등록하셨고, **웹호스팅**을 쓰고 계시다면 아래 순서대로 하시면 됩니다.

---

## 1. 가비아 로그인

1. **https://www.gabia.com** 접속
2. **로그인** (회원 아이디/비밀번호)

---

## 2. 호스팅 관리 들어가기

1. 로그인 후 **마이페이지** 또는 **서비스 관리**로 이동
2. **호스팅** / **웹호스팅** / **웹서비스** 메뉴에서  
   **connectsell.co.kr** 에 연결된 호스팅 제품 선택
3. **호스팅 관리** / **관리자** / **웹 FTP** / **파일관리자** 중 하나 선택  
   (메뉴 이름은 가비아 화면 버전에 따라 조금 다를 수 있습니다.)

---

## 3. 웹 루트(문서 루트) 확인

파일을 올리는 **기준 폴더**를 확인합니다.

- 보통 이름: **`public_html`** 또는 **`www`** 또는 **`html`** 또는 **`web`**
- 이 폴더 안에 넣은 파일이 **https://www.connectsell.co.kr/...** 로 열립니다.

예: `public_html` 이 루트라면  
→ `public_html/sample/landing.html`  
→ 주소: **https://www.connectsell.co.kr/sample/landing.html**

---

## 4. 파일 올리기 (두 가지 중 편한 방법)

### 방법 A: 파일관리자(웹 FTP)로 올리기

1. 호스팅 관리에서 **파일관리자** / **웹 FTP** 실행
2. **웹 루트 폴더**(예: `public_html`)로 이동
3. **폴더 만들기**로 `sample` 폴더 생성
4. `sample` 안에 다시 **`images`** 폴더 생성
5. **업로드** 버튼으로 아래처럼 올리기:
   - **sample 폴더 안에:**  
     `landing.html`, `landing-v3.html`, `thanks.html`, `index.html` 등  
     (필요한 HTML/CSS만 골라서 올려도 됨)
   - **sample/images 폴더 안에:**  
     `hero-creator.png`

최소한 이렇게만 있어도 샘플 페이지가 동작합니다.

```
웹 루트 (public_html 등)
└── sample/
    ├── landing.html
    ├── landing-v3.html
    ├── thanks.html
    ├── index.html
    └── images/
        └── hero-creator.png
```

### 방법 B: PC에서 FTP 프로그램으로 올리기

1. 가비아 호스팅 관리에서 **FTP 접속 정보** 확인:
   - **FTP 주소**(호스트): 예) `ftp.connectsell.co.kr` 또는 가비아에서 안내한 주소
   - **FTP 아이디 / 비밀번호**: 호스팅 계정 정보
2. **FileZilla** 등 FTP 프로그램 설치 후 위 정보로 접속
3. **원격(서버) 쪽**에서 웹 루트(예: `public_html`)로 이동
4. **로컬(내 PC)** 에서 `C:\Users\USER\connectsell` 폴더 열기
5. **`sample` 폴더 통째로** 드래그해서 서버 웹 루트 안으로 업로드  
   → 서버에 `sample/`, `sample/images/` 등이 생기면 됨

---

## 5. 올린 뒤 확인

브라우저에서 아래 주소로 접속해 봅니다.

- **https://www.connectsell.co.kr/sample/landing.html**
- **https://www.connectsell.co.kr/sample/index.html**

이미지 주소가 이미 `https://www.connectsell.co.kr/sample/images/hero-creator.png` 로 되어 있으므로,  
가비아에 `sample/images/hero-creator.png` 를 올려두면 **엑박 없이** 보입니다.

---

## 6. 이메일 HTML도 가비아에 두고 싶다면

이메일용 HTML(예: kakkukishake-email-v2.html)도 같은 호스팅에 두려면:

1. 웹 루트 아래에 **`email`** 폴더 생성
2. **`connectsell\email`** 폴더 안의 HTML 파일들과 **`email\images`** 폴더를 그대로 업로드
3. 접속 주소 예:  
   **https://www.connectsell.co.kr/email/kakkukishake-email-v2.html**

이메일에서 쓰는 이미지 주소가  
`https://www.connectsell.co.kr/email/images/...` 이라면  
가비아에도 `email/images/` 경로에 이미지들이 있어야 합니다.

---

## 정리

| 할 일 | 설명 |
|------|------|
| 가비아 로그인 | gabia.com → 로그인 |
| 호스팅 관리 | connectsell.co.kr 연결된 호스팅 → 파일관리자 또는 FTP |
| 웹 루트 확인 | public_html 등 기준 폴더 |
| sample 올리기 | sample/*.html, sample/images/hero-creator.png |
| 주소 확인 | https://www.connectsell.co.kr/sample/landing.html |

가비아는 **호스팅을 쓰는 경우**에만 위와 같이 파일을 올릴 수 있습니다.  
**도메인만** 등록하고 호스팅은 다른 곳(예: Netlify)을 쓰고 있다면, 파일은 그 **실제 호스팅**에 올려야 합니다.  
(그때는 “가비아에 추가”가 아니라 “지금 쓰는 호스팅에 sample 폴더 추가”가 됩니다.)
